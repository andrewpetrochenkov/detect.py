#!/usr/bin/env python
import detect

print("linux = %s" % detect.linux)
print("mac = %s" % detect.mac)
print("windows = %s" % detect.windows)
print("cygwin = %s" % detect.cygwin)
print("unix = %s" % detect.unix)
